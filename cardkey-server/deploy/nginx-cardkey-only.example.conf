# nginx 切流量两种方案，二选一。

# ========== 方案 A：路径级灰度（推荐先用这个）==========
# 把 location ^~ /api/plugin/card-keys/ 加到 seat.20050225.xyz 现有 server block 顶部，
# 其它路径仍走原 127.0.0.1:8000（seatpool 主服务）。
#
# location ^~ /api/plugin/card-keys/ {
#     proxy_pass http://127.0.0.1:8801;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_read_timeout 60;
# }
#
# 测试：
#   sudo nginx -t
#   sudo systemctl reload nginx
#
# 回滚：
#   sudo sed -i '/^[[:space:]]*location \^~ \/api\/plugin\/card-keys\/ {/,/^[[:space:]]*}$/d' /etc/nginx/sites-enabled/<file>
#   sudo nginx -t && sudo systemctl reload nginx


# ========== 方案 B：整 server block 替换 ==========
# 仅在确认精简版完全等价后再用。
#
# server {
#     server_name seat.20050225.xyz;
#
#     location / {
#         proxy_pass http://127.0.0.1:8801;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 60;
#     }
#
#     listen 443 ssl;
#     ssl_certificate /etc/letsencrypt/live/seat.20050225.xyz/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/seat.20050225.xyz/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
# }
